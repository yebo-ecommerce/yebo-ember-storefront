test:
  pre:
    - bower install --allow-root
  override:
    - echo "test"
deployment:
  npm:
      branch: develop
    commands:
      # login using environment variables
      - echo -e "$NPM_USERNAME\n$NPM_PASSWORD\n$NPM_EMAIL" | npm login
      - npm run 2npm
